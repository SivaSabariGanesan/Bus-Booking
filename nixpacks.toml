[phases.setup]
nixPkgs = ["python311", "python311Packages.pip"]

[phases.install]
cmds = [
    "pip install -r backend/requirements_production.txt"
]

[phases.build]
cmds = [
    "python backend/manage.py collectstatic --settings=transport_booking.settings_production --noinput"
]

[start]
cmd = "cd backend && gunicorn transport_booking.wsgi:application --bind 0.0.0.0:$PORT --workers 3 --timeout 120 --worker-class gthread --env DJANGO_SETTINGS_MODULE=transport_booking.settings_production"
